interface:
  display_name: "Linear Create Issue"
  short_description: "Create one Linear issue via API"
  brand_color: "#5E6AD2"
  default_prompt: "Use $linear-create-issue as a narrow fallback to create one Linear issue through the direct Linear API when the normal connector cannot create it, resolve team/status/project/labels first, prefer --dry-run for metadata checks, and never print the API key."

policy:
  allow_implicit_invocation: false

