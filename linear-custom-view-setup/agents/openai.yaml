interface:
  display_name: "Linear Custom View Setup"
  short_description: "Ensure a Linear Custom View exists"
  brand_color: "#5E6AD2"
  default_prompt: "Use $linear-custom-view-setup as a narrow fallback to create one required Linear Custom View through the direct Linear API, prefer --dry-run first, verify metadata, and never print the API key."

policy:
  allow_implicit_invocation: false

