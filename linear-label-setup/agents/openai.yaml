interface:
  display_name: "Linear Label Setup"
  short_description: "Ensure Linear labels exist via API"
  brand_color: "#5E6AD2"
  default_prompt: "Use $linear-label-setup as a narrow fallback to create explicitly requested missing Linear issue labels through the direct Linear API, prefer --dry-run first, and never print the API key."

policy:
  allow_implicit_invocation: false

