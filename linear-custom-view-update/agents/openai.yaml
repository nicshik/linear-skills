interface:
  display_name: "Linear Custom View Update"
  short_description: "Update one Linear Custom View"
  brand_color: "#5E6AD2"
  default_prompt: "Use $linear-custom-view-update as a narrow fallback to update one existing Linear Custom View through the direct Linear API, read before writing, prefer --dry-run first, verify after mutation, and never print the API key."

policy:
  allow_implicit_invocation: false

